{
  "name": "omnidb-workspace-frontend",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "build": "vite build",
    "watch": "vite build --watch",
    "check": "node scripts/check-bridge.mjs"
  },
  "devDependencies": {
    "vite": "^6.4.3"
  }
}
