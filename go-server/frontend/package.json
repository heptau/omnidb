{
  "name": "omnidb-workspace-frontend",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "build": "vite build --mode development --config vite.config.early.js && vite build --mode development --config vite.config.login.js && vite build --mode development",
    "build:release": "vite build --mode release --config vite.config.early.js && vite build --mode release --config vite.config.login.js && vite build --mode release",
    "check": "node scripts/check-bridge.mjs",
    "typecheck": "tsc -p jsconfig.json"
  },
  "devDependencies": {
    "@types/node": "^26.1.2",
    "typescript": "^7.0.2",
    "vite": "^6.4.3"
  }
}
