{
  "name": "omnidb-workspace-frontend",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "build": "vite build --config vite.config.early.js && vite build --config vite.config.login.js && vite build",
    "check": "node scripts/check-bridge.mjs",
    "typecheck": "tsc -p jsconfig.json"
  },
  "devDependencies": {
    "@types/node": "^26.1.2",
    "typescript": "^7.0.2",
    "vite": "^6.4.3"
  }
}
