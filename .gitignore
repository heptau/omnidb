*.log
*omnidb.db
*.DS_Store
*.idea
/build
/build_work
/.dev-home
.claude/settings.local.json
/go-server/omnidb-server
/go-server/frontend/node_modules
/wails-app/omnidb-shell
# Regenerated by `wails build -nsis` (see Makefile's _build_win_installer)
# on every Windows installer release build, then further patched in place
# by that same target -- tracking it just produces machine-specific diff
# noise with no real content change.
/wails-app/build/windows/installer/wails_tools.nsh
